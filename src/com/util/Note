 【v1.0】存子节点版本（类似递归）
 
	先判断是否存在左子节点，有就左子节点入栈。
	出栈并输出后，判断是否存在右子节点，有就存入右子节点。
	
	错误：存入的左子节点，取出后会再次访问自身左子节点，出现死循环。
	
	
【v2.0】存自身节点版本

	先判断自身是否为空，不为空就入栈。指针指向左子节点
	出栈并输出，指针指向右子节点。
	
	不需要每次判断左子节点是否存在是否要存入，而是通过【自身节点】的入栈出栈来确保【节点访问一次】，
	【子节点】来实现【节点访问一次】，会因为当【父节点】出栈时，判断从未再次访问违反了。